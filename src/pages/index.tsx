import type { NextPage } from 'next';
import IndexPage from '../components/IndexPage';

const Home: NextPage = () => {
  return (
    <div>
      <IndexPage />
    </div>
  );
};

export default Home;
